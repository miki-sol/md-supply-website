# DNS домена указывает на сервер — Caddy сам выпустит и продлит HTTPS-сертификат (Let's Encrypt)
# и будет редиректить HTTP → HTTPS.
mdsupply.by, www.mdsupply.by {
	encode zstd gzip
	reverse_proxy app:3000
}
